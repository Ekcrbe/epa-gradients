// EPA Gradients frontend entry point.
// Phase 0 placeholder — the hero displacement view, small multiples, and
// heatmap are implemented in Phases 2-3. All data is read from ./data/*.json;
// the page never calls a network API at runtime.

console.debug("EPA Gradients: scaffold loaded. Views arrive in Phase 2.");
